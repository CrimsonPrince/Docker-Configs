version: '2'

services:
  netsoc_web:
    image: httpd
    restart: unless-stopped
    environment:
      VIRTUAL_HOST: arthurcoll.com
      LETSENCRYPT_HOST: arthurcoll.com
      LETSENCRYPT_EMAIL: kingr4@protonmail.ch
    volumes:
     - /docker/r4/storage/arthurcoll.com:/usr/local/apache2/htdocs/

networks:
  default:
    external:
      name: nginx-proxy
